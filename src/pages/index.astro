---
import Layout from "../layouts/Layout.astro";
import "@/styles/globals.css";
import { getCollection } from "astro:content";
import { Badge } from "@/components/ui/badge";
import { parseISO, format } from "date-fns";
import { Chrome, FileText, Github, Globe } from "lucide-react";
import { Button } from "@/components/ui/button";

const getSlugFromPath = (path: string): string => {
  const parts = path.split("/");
  const filename = parts.pop() || "";
  return filename.replace(".md", "");
};
function formatDate(dateString: string) {
  try {
    return new Date(dateString).toLocaleDateString(undefined, {
      year: "numeric",
      month: "long",
      day: "numeric",
    });
  } catch {
    return dateString;
  }
}
let allEntries = await getCollection("posts");
const entries = allEntries
  .sort(
    (a, b) => new Date(b.data.date).valueOf() - new Date(a.data.date).valueOf(),
  )
  .filter((p) => p.data.hidden !== true);
---

<Layout
  title="Aadish Verma"
  hideName={true}
  description="A freshman and avid programmer"
>
  <main class="mx-auto">
    <table class="w-full">
      <tr>
        <th
          ><h1
            class="text-[5rem] -m-[1px] font-medium leading-[3.6rem] tracking-tighter bg-pink-500 p-3 pt-4 text-white"
          >
            aadish<br />verma
          </h1></th
        >
        <th class="px-4 flex flex-col font-normal text-lg text-start">
          <p class="">
            I’m a programmer and freshman at Stanford Online High School,
            located in the Bay Area. I'm currently working as a founding
            developer at <a href="https://www.jvcopilot.ai/" target="_blank"
              >JV Copilot</a
            >.

            <p class="flex gap-1.5">
              <a href="mailto:mail@aadishv.dev">email</a>
              •
              <a href="https://github.com/aadishv" target="_blank">github</a>
              •
              <a href="https://x.com/aadishvdotdev" target="_blank"
                >twitter (views my own)</a
              >
              •
              <a href="https://www.linkedin.com/in/aadishv/" target="_blank"
                >linkedin</a
              >
              •
              <span>discord @aadishv</span>
            </p>
          </p>
        </th>
        {
          entries.map((entry) => (
            <tr class="border-t border-gray-400/20">
              <td class="align-baseline flex">
                <time
                  datetime={parseISO(entry.data.date).toString()}
                  class="mr-2 font-mono ml-auto"
                >
                  {format(parseISO(entry.data.date), "MM.dd.yy")}
                </time>
              </td>
              <td class="align-baseline">
                <h1 class="mb-4 px-3">
                  <a
                    href={"/" + getSlugFromPath(entry.filePath ?? "")}
                    class="underline hover:decoration-none text-[2rem] !font-normal tracking-tight"
                    transition:name={entry.id}
                  >
                    {entry.data.title}
                  </a>
                </h1>
              </td>
            </tr>
          ))
        }
      </tr>

      <!-- <div class="flex flex-col lg:flex-row text-aadish border-b-2">
      <div class="text-xl mb-auto text-foreground">
        <ul class="flex flex-col gap-6 w-full mt-0">

        </ul>
      </div>
    </div> -->
    </table>
  </main>
</Layout>
